<?php /** @var $block \Vindi\VP\Block\Info\CardCard */ ?>
<?php
$info = $block->getInfo();
$additionalInfo = $info->getAdditionalInformation();
$card1Info = $block->getCard1Info();
$card2Info = $block->getCard2Info();
$paymentStatus = $block->getPaymentStatus();
?>
<div class="admin__page-section-item order-payment-method">
    <div class="admin__page-section-item-title">
        <span class="title"><?= $block->escapeHtml(__('Payment Information')) ?></span>
    </div>
    <div class="admin__page-section-item-content">
        <div class="order-payment-method-title">
            <strong><?= $block->escapeHtml(__('Method:')) ?></strong> <?= $block->escapeHtml(__('Card + Card')) ?>
        </div>
        <div class="order-payment-method-content">
            
            <?php if ($paymentStatus): ?>
                <div class="payment-method-info-row">
                    <strong><?= $block->escapeHtml(__('Payment Status')) ?>:</strong>
                    <?= $block->escapeHtml($block->formatPaymentStatus($paymentStatus)) ?>
                </div>
            <?php endif; ?>

            <!-- Card 1 Information -->
            <div class="payment-method-card-section">
                <h4><?= $block->escapeHtml(__('Card 1 Information')) ?></h4>
                
                <?php if ($card1Info['tid']): ?>
                    <div class="payment-method-info-row">
                        <strong><?= $block->escapeHtml(__('Transaction ID')) ?>:</strong>
                        <?= $block->escapeHtml($card1Info['tid']) ?>
                    </div>
                <?php endif; ?>
                
                <?php if ($card1Info['status']): ?>
                    <div class="payment-method-info-row">
                        <strong><?= $block->escapeHtml(__('Status')) ?>:</strong>
                        <?= $block->escapeHtml($card1Info['status']) ?>
                    </div>
                <?php endif; ?>
                
                <?php if ($card1Info['installments']): ?>
                    <div class="payment-method-info-row">
                        <strong><?= $block->escapeHtml(__('Installments')) ?>:</strong>
                        <?= $block->escapeHtml($card1Info['installments']) ?>x
                    </div>
                <?php endif; ?>
                
                <?php if ($card1Info['amount']): ?>
                    <div class="payment-method-info-row">
                        <strong><?= $block->escapeHtml(__('Amount')) ?>:</strong>
                        R$ <?= $block->escapeHtml(number_format((float)$card1Info['amount'], 2, ',', '.')) ?>
                    </div>
                <?php endif; ?>
            </div>

            <!-- Card 2 Information -->
            <div class="payment-method-card-section">
                <h4><?= $block->escapeHtml(__('Card 2 Information')) ?></h4>
                
                <?php if ($card2Info['tid']): ?>
                    <div class="payment-method-info-row">
                        <strong><?= $block->escapeHtml(__('Transaction ID')) ?>:</strong>
                        <?= $block->escapeHtml($card2Info['tid']) ?>
                    </div>
                <?php else: ?>
                    <div class="payment-method-info-row">
                        <strong><?= $block->escapeHtml(__('Status')) ?>:</strong>
                        <?= $block->escapeHtml(__('Pending Processing')) ?>
                    </div>
                <?php endif; ?>
                
                <?php if ($card2Info['status']): ?>
                    <div class="payment-method-info-row">
                        <strong><?= $block->escapeHtml(__('Status')) ?>:</strong>
                        <?= $block->escapeHtml($card2Info['status']) ?>
                    </div>
                <?php endif; ?>
                
                <?php if ($card2Info['installments']): ?>
                    <div class="payment-method-info-row">
                        <strong><?= $block->escapeHtml(__('Installments')) ?>:</strong>
                        <?= $block->escapeHtml($card2Info['installments']) ?>x
                    </div>
                <?php endif; ?>
                
                <?php if ($card2Info['amount']): ?>
                    <div class="payment-method-info-row">
                        <strong><?= $block->escapeHtml(__('Amount')) ?>:</strong>
                        R$ <?= $block->escapeHtml(number_format((float)$card2Info['amount'], 2, ',', '.')) ?>
                    </div>
                <?php endif; ?>
            </div>

            <!-- Additional Debug Information (if available) -->
            <?php if ($additionalInfo && count($additionalInfo) > 0): ?>
                <div class="payment-method-debug-section" style="margin-top: 15px; padding-top: 15px; border-top: 1px solid #ddd;">
                    <h5><?= $block->escapeHtml(__('Additional Information')) ?></h5>
                    <?php foreach ($additionalInfo as $key => $value): ?>
                        <?php if (!in_array($key, ['card1_payment_tid', 'card1_status', 'card1_installments', 'card1_amount', 'card2_payment_tid', 'card2_status', 'card2_installments', 'card2_amount', 'payment_status']) && !empty($value)): ?>
                            <div class="payment-method-info-row">
                                <strong><?= $block->escapeHtml($block->getLabel($key)) ?>:</strong>
                                <?= $block->escapeHtml(is_array($value) ? json_encode($value) : $value) ?>
                            </div>
                        <?php endif; ?>
                    <?php endforeach; ?>
                </div>
            <?php endif; ?>
        </div>
    </div>
</div>

<style>
.payment-method-card-section {
    margin: 10px 0;
    padding: 10px;
    background-color: #f8f8f8;
    border-left: 3px solid #007bdb;
}

.payment-method-card-section h4 {
    margin: 0 0 10px 0;
    color: #007bdb;
    font-size: 14px;
}

.payment-method-info-row {
    margin: 5px 0;
    font-size: 12px;
}

.payment-method-debug-section {
    font-size: 11px;
    color: #666;
}

.payment-method-debug-section h5 {
    margin: 0 0 10px 0;
    font-size: 12px;
    color: #333;
}
</style>
